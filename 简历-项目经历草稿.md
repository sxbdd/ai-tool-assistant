# 「AI 工具调用小助手」简历项目经历（定稿素材 v2）

> 对应聊天里确认的 **项目②：AI 工具调用 / 智能体小助手（Agent + Function Calling）**，体现"懂 Agent"。
> 说明：本项目已在 D:\Projects\AI 工具调用小助手 真实落地（Python + LangGraph + DeepSeek + Streamlit）。
> 带 【 】 的地方需按你真实数据补充后再放进简历，未补充前不写死任何成果数字。

---

## 简历条目（技术 / AI 应用向，通用写法）

### 项目经历

AI 工具调用小助手（Agent + Function Calling 智能体） ｜  独立开发  20xx.xx – 至今
- 目标：让大模型"学会调用工具"完成多步任务（读文件 / 查天气 / 整理表格），以网页对话方式交付；
- 使用 Python + LangGraph 搭建 Agent 状态机（模型节点 ↔ 工具节点条件循环），通过 Function Calling 让 DeepSeek（deepseek-chat）自主决定调用哪些工具及参数，本地 Python 负责真实执行，实现"模型决策、代码执行、结果回传"的安全闭环；
- 自研 3 个可调用工具并注册 JSON Schema：读文件 read_file、查天气 get_weather（Open-Meteo 免费接口）、整理表格 tidy_spreadsheet（pandas 清洗空行/重复行并输出 Excel 摘要）；
- 使用 Streamlit 实现网页对话界面，可展开查看每次工具调用的推理轨迹，支持多步任务一次提问自动串联完成；
- 工程规范：密钥仅存 .env（gitignore 保护）、目录按 D:\Dev 规范管理、单元测试覆盖离线工具、Git 版本管理；
- 成果（待补真实数字）：如"实现 1 个可演示的网页 Agent，支持 N 类工具调用，完成单次多步任务平均调用工具 X 次，回答可用率 Y%"。

---

## 项目真实进度（写简历前对照更新）

- [x] 项目目录与 Git 仓库初始化（D:\Projects\AI 工具调用小助手）
- [x] Agent 框架搭建：LangGraph 节点/路由、DeepSeek 接入
- [x] 三个工具实现 + Function Calling 注册
- [x] Streamlit 网页对话界面
- [x] 离线单元测试
- [ ] 填入 DeepSeek API Key 后跑通端到端演示（需要你提供 Key 或自己填 .env）
- [ ] 补充真实使用数据（调用次数 / 节省时间 / 演示对象反馈等）

---

## 需要你确认

1. 这份简历的求职方向是？（Agent 项目适合：AI 应用开发 / 大模型应用 / 算法工程实习；如仍是新媒体运营岗，可改写成"用 AI 工具做内容提效"角度——但那就不是这个 Agent 项目了，请确认。）
2. 项目起止时间？
3. 是否要把本仓库推送到你的 GitHub（sxbdd）作为简历作品链接？