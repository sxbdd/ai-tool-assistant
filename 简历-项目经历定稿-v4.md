# 「AI 工具调用小助手」简历项目经历 —— 定稿 v4（RAG + 识图 + 联网 + 多模型）

> 目标简历：D:\Administrator\桌面\简历\实习简历03.docx（张问好｜广州应用科技学院·软件工程｜求职意向：AI Agent）
> 项目代码（私有，可申请查看）：https://github.com/sxbdd/ai-tool-assistant
> 本条目全部为真实情况：功能已实现并通过端到端验证（真实调用 DeepSeek / 智谱识图 / Bing 搜索）。

---

## 项目经历（可直接粘贴）

AI 工具调用小助手（Agent 智能体：RAG + 工具调用 + 识图 + 联网搜索）　｜　独立开发　2026.09 – 至今
● 项目目标：做一个"会查私有资料、会看图、会联网、会调工具"的网页智能体，让大模型（DeepSeek）完成读文件 / 查天气 / 整理表格 / 知识库问答等多步任务，以 Streamlit 网页对话交付；
● 基于 LangGraph 实现 Agent 状态机：agent 节点由模型决策是否调工具，tools 节点本地真实执行并回传结果，条件路由自动循环直至产出最终答案；支持一次提问并行调用多个工具；
● 实现 RAG 知识库问答：支持上传 PDF/文本/表格与导入本地文件夹，文档切分后经中文向量模型（bge-small-zh）入库 Chroma，检索带"文件·页码"出处并加相似度阈值防幻觉；
● 实现多模态识图：自研"视觉桥" analyze_image，文本主模型不会看图时自动调免费视觉模型（智谱 GLM-4V-Flash）把图片转成文字描述；模型层做成 Provider 注册表，DeepSeek / 智谱 / 通义 / OpenAI 可下拉切换并精确到具体型号；
● 实现联网搜索：接入 Bing 免费搜索（可选 Tavily），支持"搜索 → 打开网页细读 → 综合带链接回答"的自主调研流程；
● 工程与体验：共 7 个可调用工具；API Key 仅存 .env（gitignore）；离线单测 12/12 通过；端到端验证通过（知识库问答 / 两地天气并行 / 表格清洗 / 图片识图 / 联网调研）；UI 采用折叠面板 + 附件式上传 + 文件库管理，向 Codex 桌面版交互靠齐；
● 使用 Git 全程版本管理，代码已推送 GitHub（私有仓库可提供查看）。

---

## 面试口述要点（背熟）

1. **Agent 循环**：模型决策 ↔ 工具执行 ↔ 结果回传，条件路由决定继续还是结束；多步任务自动串联。
2. **RAG 流程**：切分 → 向量化(bge-small-zh) → Chroma 检索 → 只依据片段+出处生成；相似度阈值防止"查不到硬答"。
3. **视觉桥 vs 直连多模态**：文本模型省钱省量，看图时才调免费视觉模型转文字；有需要也可把主力切成视觉大模型。
4. **模型抽象层**：OpenAI 兼容协议，换 base_url+model+key 即换服务商；UI 可运行时切换，视觉桥实时生效。
5. **联网搜索**：什么时候用知识库、什么时候用 web_search，由 Agent 判断；搜索结果必须附链接、不臆造。
6. **安全与工程**：写入只在 data/ 沙盒；Key 只进 .env；文件夹导入只读+复制进沙盒；单测/回归保证不坏老功能。

---

## 真实验证记录（写简历/被追问时引用）

- 离线单测：12/12 通过（切分/入库/检索/越界过滤/文件/表格/Bing 解析）
- 端到端（真调 API）：①读文件 ②知识库问答（按笔记解释 RAG，带出处）③北京上海天气并行 ④表格 7→5 行清洗分析 ⑤识图准确读出图内中文与图形 ⑥Agent 自主 web_search→open_webpage×2→综合回答
- 网页：Streamlit AppTest 无头渲染通过；折叠面板/附件上传/文件库预览可用