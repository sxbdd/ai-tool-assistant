# 「AI 工具调用小助手」简历项目经历 —— 定稿 v3（张问好 · 求职意向：AI Agent）

> 目标简历：D:\Administrator\桌面\简历\实习简历03.docx（张问好｜广州应用科技学院·软件工程｜求职意向：AI Agent）
> 本条目内容全部为真实情况：项目已实现并通过端到端验证（真实调用 DeepSeek，3 类用例跑通）。
> 唯一建议微调：如后续有真实使用数据（他人试用反馈、接入更多工具），可再补一句量化成果。

---

## 项目经历（可直接粘贴）

AI 工具调用小助手（Agent + Function Calling 智能体）　｜　独立开发　2026.09 – 至今
● 项目目标：让大模型（DeepSeek）学会调用工具完成多步任务（读文件 / 查天气 / 整理表格），并以网页对话方式交付；
● 基于 LangGraph 实现 Agent 状态机：agent 节点由模型判断是否调用工具，tools 节点在本地真实执行并回传结果，条件路由自动循环，单次任务可串联多次工具调用直至产出最终答案；
● 通过 Function Calling 向模型注册 3 个 JSON Schema 工具：read_file（读文件）、get_weather（调用 Open-Meteo 免费接口查实时天气）、tidy_spreadsheet（用 pandas 清洗空行/重复行，输出 Excel 与数据摘要）；
● 使用 Streamlit 搭建网页对话界面，可实时展开查看每一步工具调用轨迹，便于演示与学习；
● 工程规范：DeepSeek API Key 仅存 .env（gitignore 保护）、目录结构规范、编写离线单元测试（3/3 通过）、全程 Git 版本管理；
● 已端到端验证（真实调用 DeepSeek）：能自动读取文件并总结；一次提问并行查询北京、上海两地实时天气并对比分析；自动识别表格中的空行与重复行问题，输出清洗报告（原始 7 行 → 有效 5 行）。

---

## 面试时能展开讲的点（加分，建议背熟）

1. **Agent 是什么**：大模型负责"推理决策"，工具负责"真实执行"，Agent = 决策 + 工具 + 循环，解决大模型"只会说不会做"的问题。
2. **Function Calling 原理**：模型不直接执行代码，而是从工具注册表（JSON Schema）里选工具、生成参数；本地校验并执行，结果作为消息回传，模型据此继续推理——可控、可解释、安全。
3. **LangGraph 状态机**：节点 = agent(决策) / tools(执行)；边 = 条件路由（还有工具调用 → tools，否则 → END）；消息用 add_messages 自动累积。
4. **为什么安全**：工具只允许访问项目 data 目录，不碰系统/应用数据；Key 只在 .env，不入库。
5. **为什么用 Open-Meteo**：免费、无需 Key、返回 JSON，适合演示；生产可换更专业的天气 API。